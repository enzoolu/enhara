spring:
  datasource:
    url: jdbc:h2:mem:enhara-test;MODE=PostgreSQL;DATABASE_TO_LOWER=TRUE;DB_CLOSE_DELAY=-1;DEFAULT_NULL_ORDERING=HIGH
    username: sa
    password:
    driver-class-name: org.h2.Driver
enhara:
  demo:
    seed: false
    auto-start-simulation: false
  vehicle-photos:
    directory: target/test-vehicle-photos
